name: todo_with_freezed_bloc
description: Project to learn BLoC and freezed.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^7.2.1
  flutter_bloc: ^7.3.3
  cupertino_icons: ^0.1.3
  freezed_annotation: ^0.15.0
  json_annotation: ^4.3.0
  uuid: 3.0.4
  hive: ^2.0.4
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4
  logger: ^1.1.0
  build_runner: ^2.1.5
  freezed: ^0.15.1+1
  json_serializable: ^6.0.1
  hive_generator: ^1.1.0

flutter:
  uses-material-design: true

